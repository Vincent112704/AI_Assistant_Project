todo:
    - Test whether simple agent is working
    - create a docx to pdf service because telegram sendDocument api only accepts PDF or ZIP 
    - Finalize the create_docx_tool on whether you should separate the logic and the tool as 
      they seem kinda redundant right now
    - register all tools in registry.py
    - set up firebase
    - create the telegram_push_message_service 
    - set up caching 
    - set up queueing system for jobs (seems kinda overkill for a personal use project but just add it anyway)
      explore RabbitMQ this

Notes(2026-05-04):
  - Issue(Docker build) [FIXED]: 
    - Windows-only transitive dependency (pywin32) can break Linux Docker builds even if not directly installed
    - Common chain: openai-agents → mcp → pywin32 or docx2pdf → pywin32
    - Root cause is platform incompatibility, not Poetry/pip issue
    - Dependency resolvers include packages based on compatibility, but do not guarantee runtime OS match
    - Debug via dependency tree (poetry show --tree or pipdeptree -r -p pywin32 (reverse tree look up))
    - Fix by removing Windows-specific packages or isolating them via OS markers (sys_platform == "win32")
    - You might have to use alternatives for docx2pdf because of OS incompatibility (check on other libraries that supports linux)
  - Issue(telegram_push_message_service)[FIXED]: 
    - ERROR:root:Failed to send document. Status code: 403, Response: {"ok":false,"error_code":403,"description":"Forbidden: bots can't send messages to bots"}
    - Might have something to do with the chat_id passed. Verify chat_id
    - review documentation on the /sendDocument endpoint
  
Notes(2025-05-15):
  -Issue(simple_agent.py):
    - TypeError: create_agent() takes from 1 to 2 positional arguments but 3 were given
    - Search on create_agent() on why the arguments we passed are wrong
    
